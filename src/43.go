package main

import "fmt"

func main() {
    var name string = "Student"
    var age int = 18
    var isMarked bool = true

    fmt.Println("Hello", name, ", you are", age, "years old and have been marked as", isMarked)
}
